<p id="notice"><%= notice %></p>
<% @activity = Activity.find(params[:id]) %>
<p>
  <b>Date:</b>
  <%= @event.date %>
</p>

<p>
  <b>Activity:</b>
  <%= @activity.title %>
  <%= image_tag @activity.image_url(:minithumb).to_s %>
</p>


<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
