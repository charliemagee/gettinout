<% provide(:title, 'Our Users') %>
<div class="center hero-unit">
  <div>
    <h2>Our Users</h2>
    <% @users = User.all %>
		<% @users.each do |user| %>
			<%= image_tag user.image_url(:thumb).to_s %>
		  <%= user.email %><br/>
		<% end %>
  </div>
</div>